'use strict';

describe('myApp.cart module', function() {

  beforeEach(module('myApp.cart'));

  describe('Cart controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cartCtrl = $controller('CartCtrl');
      expect(cartCtrl).toBeDefined();
    }));

  });
});